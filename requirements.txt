# Core Bayesian MMM stack — pinned for numpy compatibility
# numpy<2.0 required to avoid conflicts with sagemaker / pywavelets
numpy>=1.24.0,<2.0
pymc>=5.0.0
pytensor>=2.18.0
arviz>=0.17.0

# Optimization & data
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
