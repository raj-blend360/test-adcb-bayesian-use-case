# Bayesian MMM + Optimization stack
# pandas<3 required to avoid conflicts with mlflow / streamlit in the environment
# numpy>=2.0 is fine — sagemaker/pywavelets not required

numpy>=2.0
pymc>=5.10.0
pytensor>=2.26.0
arviz>=0.18.0
scipy>=1.13.0
pandas>=2.2.0,<3.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
